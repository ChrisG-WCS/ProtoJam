import styles from './LivesDisplay.module.css';

const LivesDisplay = () => (
  <div className={styles.livesContainer}>
    <span className={styles.hearts}>❤️❤️❤️</span>
  </div>
);

export default LivesDisplay;
